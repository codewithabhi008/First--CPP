#include <iostream>

using namespace std;

int main(){
    int a, b, c, d, sum;

//  variable of two number

    cout << "Enter two numbers: ";
    cin >> a >> b;

    sum = a + b;

    cout << "Sum = " << sum;

    // Variable of three number
    cout << "Enter three numbers:";
    cin >> a >> b >> c;

    sum = a + b + c;

    cout << " Sum = " << sum;

    // Variable of four number
    cout << "Enter four number: ";
    cin >> a >> b >> c >> d;

    sum = a + b + c + d;

    cout << " Sum = " << sum;


    // Variable of two number with Multiplication

    cout << "Enter two number: ";
    cin >> a >> b;

    sum = a * b;

    cout << " Sum = " << sum;

    //  // Variable of three number with Multiplication

     cout << "Enter three number: ";
     cin >>  a >> b >> c;

     sum = a * b * c;

     cout << "Sum = " << sum;

    // Variable of Four number with Multiplication

     cout << "Enter four number: ";
     cin >>  a >> b >> c >> d;

     sum = a * b * c * d;

     cout << "Sum = " << sum;


    return 0;
}